extends layout

block content
  #wrapper
    #logo
      img(src='/images/logo.jpg')
    #display
      include userbar
        
      -for (var id in items)
        - var item = items[id]
        div
          a(href='/PadList/#{id}') #{item.url}
      
    include footer